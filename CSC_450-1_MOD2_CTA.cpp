#include <iostream>

using namespace std;

int main()
{
    // Declare three string variables
    // Two to store user input
    // One to store the concatenated result
    string input1;
    string input2;
    string result;

    // Prompt for and record user input
    cout << "String 1: ";
    cin >> input1;
    cout << "String 2: ";
    cin >> input2;

    // Concatenate the two inputted strings
    result = input1 + input2;

    // Output the concatenated strings
    cout << "Concatenated Strings: " + result << endl;

    // Prompt for and record user input
    cout << "String 1: ";
    cin >> input1;
    cout << "String 2: ";
    cin >> input2;

    // Concatenate the two inputted strings
    result = input1 + input2;

    // Output the concatenated strings
    cout << "Concatenated Strings: " + result << endl;

    // Prompt for and record user input
    cout << "String 1: ";
    cin >> input1;
    cout << "String 2: ";
    cin >> input2;

    // Concatenate the two inputted strings
    result = input1 + input2;

    // Output the concatenated strings
    cout << "Concatenated Strings: " + result << endl;

    return 0;
}
